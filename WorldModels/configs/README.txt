Config files for the World Models repo
carracing.config and doom.config are legacy from the original fork

multiwalker.config is new and meant to configure the multiwalker env and World Models components (e.g. RNN/controller/etc.)